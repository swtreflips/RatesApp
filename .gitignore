node_modules
dist
.env.local
.env*.local
*.local

# Agent skill tooling (local dev only)
.agents/
.claude/
skills-lock.json

# Python (graph.py reference / Graph send tooling)
__pycache__/
*.pyc
.graph_refresh_token
/msal/          # local virtualenv for graph.py (msal/requests/openpyxl)

# Edge Function fill verification output (supabase/functions/_shared/fillTemplate.test.ts)
out.xlsx
.vercel
.env*
.skins-out/
